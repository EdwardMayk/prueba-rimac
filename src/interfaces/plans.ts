export interface CardProps {
  icon: string;
  title: string;
  description: string;
  isSelected: boolean;
}

export interface PlanProps {
  name: string;
  price: number;
  description: string[];
  age: number;
}

